Para executar a instalação automática basta copiar o conteúdo deste diretório para a pasta home do usuário. A seguir, execute o script install.sh no modo super usuário:

$ cd auto_install
$ cp * ~
$ cd ~
$ sudo install.sh

O script se encarregará de realizar as instalações necessárias.

Para testar se a instalação ocorreu corretamente, teste com os programas:

python3 teste.py
python3 NeuroLib.py
python3 NeuroFrame.py
